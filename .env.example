PORT=3000
CORS_ORIGIN=http://localhost:4200

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=smart_expense_uae
DB_SSL=false
DB_LOGGING=false
DB_SYNCHRONIZE=true
DB_RUN_MIGRATIONS=false

JWT_ACCESS_SECRET=super-secret-access
JWT_REFRESH_SECRET=super-secret-refresh
JWT_ACCESS_EXPIRES_IN_SECONDS=900
JWT_REFRESH_EXPIRES_IN=7d

ACCRUAL_REMINDER_OFFSET_DAYS=2
ACCRUAL_AMOUNT_TOLERANCE=5

SUPER_ADMIN_EMAIL=kunalbajaj19@outlook.com
SUPER_ADMIN_PASSWORD=EverGreen@11
SUPER_ADMIN_NAME=Platform Admin

# Email to receive new registration notifications (defaults to kunalbajaj19@outlook.com if not set)
SUPER_ADMIN_NOTIFICATION_EMAIL=kunalbajaj19@outlook.com
# Storage Configuration (AWS S3 or Cloudflare R2)
# Set STORAGE_TYPE to 's3' for AWS S3 or 'r2' for Cloudflare R2
STORAGE_TYPE=r2

# Cloudflare R2 Configuration
R2_ACCOUNT_ID=your-r2-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-r2-bucket-name
# Optional: Custom public URL for R2 (if using custom domain)
# R2_PUBLIC_URL=https://your-custom-domain.com

# AWS S3 Configuration (if using S3 instead of R2)
# AWS_S3_REGION=ap-south-1
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_S3_BUCKET=your-s3-bucket-name
